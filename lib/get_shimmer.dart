/// Get Shimmer
///
/// Public export for the package. Re-exports the main implementation.
library get_shimmer;

export 'main.dart';

// Keep this file small: it acts as the package barrel so users can import
// `package:get_shimmer/get_shimmer.dart` and get the public API.
